from flask import render_template, Blueprint #and any other
#from event_reg.RS.forms import <form name>

RS = Blueprint('RS',__name__)

#create route
#create function():
    #any necessary options if needed

    #return html file using render_template

#routes needed for home, events, register(form here), register success(ack mail sent here)